<?xml version="1.0" encoding="utf-8"?>
<!-- Panel nen toi + vien cyan glow. Opaque de giam overdraw; glow tu stroke. Wave 1 F3. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/neon_bg_surface" />
    <corners android:radius="@dimen/neon_corner" />
    <stroke
        android:width="@dimen/glow_stroke"
        android:color="@color/neon_cyan_glow" />
</shape>
