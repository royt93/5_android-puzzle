<?xml version="1.0" encoding="utf-8"?>
<androidx.appcompat.widget.Toolbar xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/tbBoardOptions"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:theme="@style/AppToolbarTheme"
    android:titleTextColor="@color/neon_text_primary"
    android:subtitleTextColor="@color/neon_text_secondary">

    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/fragment_board_size_spinner"
        android:name="com.helpmepls.slidepuzzle.frm.BoardSizeSpinnerFrm"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="end" />
</androidx.appcompat.widget.Toolbar>
